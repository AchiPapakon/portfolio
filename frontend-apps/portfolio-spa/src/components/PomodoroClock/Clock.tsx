import { Box, styled } from '@mui/material';

const Clock = styled(Box)(() => ({
    width: 140,
    border: '4px solid black',
    borderRadius: '6px',
    fontSize: '32px',
    textAlign: 'center',
}));

export default Clock;
